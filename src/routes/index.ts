import { router_record } from "./record";
import { router_users } from "./users";
import { route_zone } from "./zone";

export const users = router_users;
export const zone = route_zone;
export const record = router_record;